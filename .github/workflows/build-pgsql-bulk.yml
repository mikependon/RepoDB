name: Build & Run Tests - RepoDb.PostgreSql.BulkOperations (Push)

on:
  push:
    paths:
      - 'RepoDb.Core/**'
      - 'RepoDb.PostgreSql/**'
      - 'RepoDb.Extensions/RepoDb.PostgreSql.BulkOperations/**'
      - '.github/workflows/_base.yml'
      - '.github/workflows/build-pgsql-bulk.yml'

jobs:
  build:
    uses: ./.github/workflows/_base.yml
    with:
      solution-dir: RepoDb.Extensions/RepoDb.PostgreSql.BulkOperations
      solution-file: RepoDb.PostgreSql.BulkOperations.sln
      artifact-name: release-pgsql-bulk
