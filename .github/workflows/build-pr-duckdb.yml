name: PR - Build & Run Tests - RepoDb.DuckDb

on:
  workflow_dispatch:
  pull_request:
    paths:
      - 'src/Providers/RepoDb.Core/**'
      - 'src/Providers/RepoDb.DuckDb/**'
      - '.github/workflows/build-base.yml'
      - '.github/workflows/build-pr-duckdb.yml'

jobs:
  build:
    uses: ./.github/workflows/build-base.yml
    with:
      solution-dir: src/Providers/RepoDb.DuckDb
      solution-file: RepoDb.DuckDb.sln
      artifact-name: release-pr
      db-engine: none
