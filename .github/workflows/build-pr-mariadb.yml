name: PR - Build & Run Tests - RepoDb.MariaDb

on:
  workflow_dispatch:
  pull_request:
    paths:
      - 'src/Providers/RepoDb.Core/**'
      - 'src/Providers/RepoDb.MariaDb/**'
      - '.github/workflows/build-base.yml'
      - '.github/workflows/build-pr-mariadb.yml'

jobs:
  build:
    uses: ./.github/workflows/build-base.yml
    with:
      solution-dir: src/Providers/RepoDb.MariaDb
      solution-file: RepoDb.MariaDb.sln
      artifact-name: release-pr
      db-engine: mariadb
