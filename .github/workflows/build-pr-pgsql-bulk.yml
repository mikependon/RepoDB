name: PR - Build & Run Tests - RepoDb.PostgreSql.BulkOperations

on:
  workflow_dispatch:
  pull_request:
    paths:
      - 'src/Providers/RepoDb.Core/**'
      - 'src/Providers/RepoDb.PostgreSql/**'
      - 'src/Providers/RepoDb.PostgreSql.BulkOperations/**'
      - '.github/workflows/build-base.yml'
      - '.github/workflows/build-pr-pgsql-bulk.yml'

jobs:
  build:
    uses: ./.github/workflows/build-base.yml
    with:
      solution-dir: src/Providers/RepoDb.PostgreSql.BulkOperations
      solution-file: RepoDb.PostgreSql.BulkOperations.sln
      artifact-name: release-pr
      db-engine: postgresql
