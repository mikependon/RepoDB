name: PR -  Build & Run Tests - RepoDb.SapHana

on:
  workflow_dispatch:
  pull_request:
    paths:
      - 'src/Providers/RepoDb.Core/**'
      - 'src/Providers/RepoDb.SapHana/**'
      - '.github/workflows/build-base.yml'
      - '.github/workflows/build-pr-saphana.yml'

jobs:
  build:
    uses: ./.github/workflows/build-base.yml
    with:
      solution-dir: src/Providers/RepoDb.SapHana
      solution-file: RepoDb.SapHana.sln
      artifact-name: release-pr
      db-engine: saphana
