name: Build & Run Tests - RepoDb.SQLite.System (Pull Request)

on:
  workflow_dispatch:
  pull_request:
    paths:
      - 'RepoDb.Core/**'
      - 'RepoDb.SQLite.System/**'
      - '.github/workflows/build-base.yml'
      - '.github/workflows/build-pr-sqlite-system.yml'

jobs:
  build:
    uses: ./.github/workflows/build-base.yml
    with:
      solution-dir: RepoDb.SQLite.System
      solution-file: RepoDb.SQLite.System.sln
      artifact-name: release-pr
