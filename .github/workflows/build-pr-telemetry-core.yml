name: Build & Run Tests - RepoDb.Telemetry.Core (Pull Request)

on:
  workflow_dispatch:
  pull_request:
    paths:
      - 'RepoDb.Core/**'
      - 'RepoDb.Telemetry.Core/**'
      - '.github/workflows/build-base.yml'
      - '.github/workflows/build-pr-telemetry-core.yml'

jobs:
  build:
    uses: ./.github/workflows/build-base.yml
    with:
      solution-dir: RepoDb.Telemetry.Core
      solution-file: RepoDb.Telemetry.Core.sln
      artifact-name: release-pr
