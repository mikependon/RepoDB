name: PR - Build & Run Tests - RepoDb.Telemetry.Default

on:
  workflow_dispatch:
  pull_request:
    paths:
      - 'RepoDb.Core/**'
      - 'RepoDb.Telemetry.Core/**'
      - 'RepoDb.Telemetry.Default/**'
      - '.github/workflows/build-base.yml'
      - '.github/workflows/build-pr-telemetry-default.yml'

jobs:
  build:
    uses: ./.github/workflows/build-base.yml
    with:
      solution-dir: RepoDb.Telemetry.Default
      solution-file: RepoDb.Telemetry.Default.sln
      artifact-name: release-pr
