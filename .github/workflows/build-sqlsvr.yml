name: Build & Run Tests - RepoDb.SqlServer (Push)

on:
  workflow_dispatch:
  push:
    paths:
      - 'src/Providers/RepoDb.Core/**'
      - 'src/Providers/RepoDb.SqlServer/**'
      - '.github/workflows/build-base.yml'
      - '.github/workflows/build-sqlsvr.yml'

jobs:
  build:
    uses: ./.github/workflows/build-base.yml
    with:
      solution-dir: src/Providers/RepoDb.SqlServer
      solution-file: RepoDb.SqlServer.sln
      artifact-name: release-sqlsvr
      db-engine: sqlserver
